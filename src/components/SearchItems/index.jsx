import styles from "./styles.module.scss";

const index = () => {
  return (
    // Add icon inside
    <input
      placeholder="Search..."
      className={styles.root}
    />
  )
}
export default index